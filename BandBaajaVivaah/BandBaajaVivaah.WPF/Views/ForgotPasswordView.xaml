<Window x:Class="BandBaajaVivaah.WPF.Views.ForgotPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views"
        mc:Ignorable="d"
        Title="Forgot Password" Height="350" Width="550"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Images/ForgotPasswordFavicon.ico">
    <Grid Margin="20">
        <StackPanel >
            <Label Content="Enter your email address to receive a password reset token." Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0" Style="{StaticResource Styles.TextBox}"/>
            <Button Content="Send Reset Email" Command="{Binding SendResetEmailCommand}" Margin="0,20,0,0" Style="{StaticResource Styles.Button}"/>
            <Label Content="{Binding Message}" Foreground="Green" HorizontalAlignment="Center" Margin="0,10,0,0" Style="{StaticResource Styles.Label}"/>
        </StackPanel>
    </Grid>
</Window>
