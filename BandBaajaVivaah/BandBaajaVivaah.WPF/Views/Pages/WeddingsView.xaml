<Page x:Class="BandBaajaVivaah.WPF.Views.Pages.WeddingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WeddingsView"
      Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My Weddings" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add New Wedding" Command="{Binding AddWeddingCommand}" Margin="0,0,10,0"/>
            <Button Content="Edit Selected" Command="{Binding EditWeddingCommand}" Margin="0,0,10,0"/>
            <Button Content="Delete Selected" Command="{Binding DeleteWeddingCommand}" Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Weddings}" 
                  SelectedItem="{Binding SelectedWedding}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Wedding Name" Binding="{Binding WeddingName}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding WeddingDate, StringFormat='d'}" Width="150"/>
                <DataGridTextColumn Header="Budget" Binding="{Binding TotalBudget, StringFormat='C'}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
