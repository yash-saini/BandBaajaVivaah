<UserControl x:Class="BandBaajaVivaah.WPF.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800"
             Background="Gold">
    <Grid>
        <mah:DropDownButton
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            Margin="0,0,20,0"
            Style="{StaticResource Styles.ProfileButton}"
            ArrowVisibility="Collapsed">

            <mah:DropDownButton.Content>
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="AccountCircle" Width="24" Height="24" VerticalAlignment="Center"/>
                    <Label Content="{Binding CurrentUserName}" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="14" Style="{StaticResource Styles.Label}"/>
                </StackPanel>
            </mah:DropDownButton.Content>

            <mah:DropDownButton.Items>
                <MenuItem Header="Logout" Command="{Binding LogoutCommand}" Style="{StaticResource Styles.MenuItem}" Background="Gold"/>
            </mah:DropDownButton.Items>

        </mah:DropDownButton>
    </Grid>
</UserControl>
