<Window x:Class="BandBaajaVivaah.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Login" Height="350" Width="400">
    <Grid Margin="20" VerticalAlignment="Top">
        <StackPanel VerticalAlignment="Top">
            <Label Content="Email:" Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Label Content="Password:" Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,20,0,0" Style="{StaticResource Styles.Button}"/>

            <Label Content="{Binding ErrorMessage}" Foreground="Red" Style="{StaticResource Styles.Label}" HorizontalAlignment="Center" Margin="0,10,0,0"/>

            <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock.Inlines>
                    <Run Text="Don't have an account? "/>
                    <Hyperlink Command="{Binding RegisterCommand}">
                        <Run Text="Register here"/>
                    </Hyperlink>
                </TextBlock.Inlines>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
