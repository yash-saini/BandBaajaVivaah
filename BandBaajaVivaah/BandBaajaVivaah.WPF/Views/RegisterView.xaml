<Window x:Class="BandBaajaVivaah.WPF.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views"
        mc:Ignorable="d"
        Title="Register" Height="450" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock HorizontalAlignment="Left" Margin="0,0,0,15">
                <Hyperlink Name="BackToLoginLink" Click="BackToLoginLink_Click">
                    &lt; Back to Login
                </Hyperlink>
            </TextBlock>
            <Label Content="Full Name:" Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Label Content="Email:" Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Label Content="Password:"  Style="{StaticResource Styles.Label}"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Button Content="Register" Command="{Binding RegisterCommand}" Margin="0,20,0,0" Style="{StaticResource Styles.Button}"/>

            <Label Content="{Binding Message}" Foreground="Green" HorizontalAlignment="Center" Margin="0,10,0,0"  Style="{StaticResource Styles.Label}" />
        </StackPanel>
    </Grid>
</Window>
