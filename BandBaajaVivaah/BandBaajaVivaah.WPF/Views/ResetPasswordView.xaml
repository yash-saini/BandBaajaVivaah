<Window x:Class="BandBaajaVivaah.WPF.Views.ResetPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BandBaajaVivaah.WPF.Views"
        mc:Ignorable="d"
        Title="Reset Your Password" Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Images/ResetPasswordFavicon.ico">
    <Grid Margin="20">
        <StackPanel>
            <Label Content="Email Address:" Style="{StaticResource  Styles.Label}"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Label Content="Reset Token (from email):" Style="{StaticResource  Styles.Label}"/>
            <TextBox Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Label Content="New Password:" Style="{StaticResource  Styles.Label}"/>
            <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Styles.TextBox}"/>

            <Button Content="Reset Password" Command="{Binding ResetPasswordCommand}" Margin="0,20,0,0" Style="{StaticResource Styles.Button}"/>

            <Label Content="{Binding Message}" Foreground="Green" HorizontalAlignment="Center"  Margin="0,10,0,0" Style="{StaticResource Styles.Label}"/>
        </StackPanel>
    </Grid>
</Window>
